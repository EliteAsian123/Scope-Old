import "Standard/Core";
import "Standard/Math/Factors";

int primes = 0;
for (int i = 0, true, i++) {
	if (Math.isPrime((long) i)) {
		primes++;
	}
	
	if (primes == 10001) {
		println((string) i);
		break;
	}
}
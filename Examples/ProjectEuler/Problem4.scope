#insert Standard/Core.scope
#insert Standard/Math.scope

// Not working

for (int i = 1, true, i++) {
	bool isAnswer = true;
	for (int m = 1, m <= 20, m++) {
		if (i % m != 0) {
			isAnswer = false;
			break;
		}
	}

	if (isAnswer) {
		println((string) i);
		break;
	}
}
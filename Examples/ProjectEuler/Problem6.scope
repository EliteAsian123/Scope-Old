#insert Standard/Core.scope

long sumOfSquares = 0l;
for (long i = 1l, i <= 100l, i += 1l) {
	sumOfSquares += i ^ 2l;
}

long squareOfSum = 0l;
for (long i = 1l, i <= 100l, i += 1l) {
	squareOfSum += i;
}
squareOfSum = squareOfSum ^ 2l;

println((string) (squareOfSum - sumOfSquares));
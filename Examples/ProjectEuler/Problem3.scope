#insert Standard/Core.scope
#insert Standard/Math.scope

long number = 600851475143l;
long[] factors = factorsOf(number);

long largest = -1l;
for (int i = 0, i < factors.length, i++) {
	if (factors[i] > largest && isPrime(factors[i])) {
		largest = factors[i];
	}
}

println((string) largest);
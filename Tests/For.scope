import "Standard/Core";

for (int i = 0, i < 5, i++) {
	println("Loop " + (string) i);
}
println("For loop finished.");
println("");

for (int i = 0, i < 100, i++) {
	if (i >= 5) {
		println("Nope.");
		break;
	}
	
	println("Long Loop? " + (string) i);
}

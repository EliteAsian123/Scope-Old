#insert Standard/Core.scope

int[] intArray = new int[] {
	12,
	-3523,
	-48600843,
	0,
	234246
};

for (int i = 0, i < intArray.length, i++) {
	println((string) intArray[i]);
}

string[] stringArray = new string[intArray[0]];

for (int i = 0, i < stringArray.length, i++) {
	stringArray[i] = (string) (i * 3);
}

for (int i = 0, i < stringArray.length, i++) {
	println(stringArray[i]);
}